package util

object Constants {
  val PartialAuthUsername = "partialAuthUsername"
  val RedirectString = "redirect"
  val XForwardedFor = "X-Forwarded-For"
  val PartialAuthTimeout = "partialAuthTimeout"
}
